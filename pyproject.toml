[project]
name = "document-pipeline"
version = "0.1.0"
description = "Event-sourced primitives for AI-driven document processing"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
